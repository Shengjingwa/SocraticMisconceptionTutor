- [x] 确保 `src/main.py` 的对话结束条件（`understanding_verified`）不再过早触发（至少需要经过一次 `S6` 状态的提问，且下一轮学生认知状态被判定为 `概念掌握验证`）。
- [x] 确保 `src/simulator.py` 中 `max_turns` 修改为 10，允许更深度的交互。
- [x] 确保 `src/simulator.py` 中的系统提示词包含“除非老师拿出了具体的物理现象或严密的逻辑推导，否则不要轻易说自己懂了”等抗引导指令。