# Tasks

- [ ] Task 1: 优化 `src/generator.py` 中的提示词结构
  - [ ] SubTask 1.1: **破解机械共情**。重写 `empathy_scaffolding` 变量的文本内容，严禁使用“没关系”、“别着急”等生硬套话，要求大模型必须通过指出物理概念本身容易混淆或反直觉的地方来建立“认知共情”，并将共情与下一个引导问题无缝融合。
  - [ ] SubTask 1.2: **化解类比死锁**。更新护栏规则中的第 6 条，加入“类比熔断”指令：如果学生对类比产生误解或排斥，大模型必须“绝对不要去解释或为该类比辩护”，要大方承认其局限性并强制将视角拉回目标物理情境本身或极端条件。
  - [ ] SubTask 1.3: **攻克深度认知冲突**。在 `fallback_strategy` 构建逻辑处，为 `decision.state == "S4"` 或 `S5` 连续卡壳的状态，动态追加一段【深度认知冲突策略】的提示词，要求模型强制采用“归谬法（Reductio ad absurdum）”或“极端情境法”，顺应学生的错误逻辑推导出一个荒谬后果。

- [ ] Task 2: 放宽 `src/guardrails.py` 中 LLM Judge 的拦截标准
  - [ ] SubTask 2.1: **特别豁免归谬法**。修改 `check_output` 中的 `judge_prompt`，在“豁免标准（不违规）”中明确添加一条声明：“如果助教是‘顺着学生的错误物理概念，推导出一个明显荒谬的后果或极端现象’以此来制造认知冲突，只要没有直接告诉学生最终的正确科学结论，则属于优秀的启发式教学，绝对不属于违规。”

# Task Dependencies
- Task 1 和 Task 2 相对独立，但共同服务于提升主观教学质量指标。由于这是纯文本提示词的优化，无需复杂的逻辑测试即可提交。