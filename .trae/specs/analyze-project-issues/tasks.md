# Tasks
- [x] Task 1: 验证 `PROJECT_ISSUES.md` 中的问题真伪
  - [x] SubTask 1.1: 验证工程问题（例如：是否真的只有 `test.ipynb`，是否有模块化，是否有重试容错机制，是否有依赖管理 `requirements.txt`）。
  - [x] SubTask 1.2: 验证算法/策略问题（例如：当前是否还是单纯的 Top-k 向量检索，是否已经加入了 FSM 状态机和学生模型）。
  - [x] SubTask 1.3: 验证教育学/评估问题（例如：是否仍然缺乏实验设计，是否进行了质量控制，是否有 LLM Judge）。

- [x] Task 2: 制定短中长期解决清单
  - [x] SubTask 2.1: 总结真实存在的问题，并按照短期（补齐依赖/文档）、中期（丰富学生模型与测试）、长期（人类实验/扩展学科）进行归类。
  - [x] SubTask 2.2: 将分析结果输出给用户。